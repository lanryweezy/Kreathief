--- tests/e2e/visual/visual-regression.spec.ts
+++ tests/e2e/visual/visual-regression.spec.ts
@@ -23,12 +23,8 @@

     // Navigate to editor
-    await page.goto('/');
-    await expect(page.locator('#templates-grid')).toBeVisible({ timeout: 10000 });
-    const firstTemplateBtn = page.locator('#templates-grid button').first();
-    if (await firstTemplateBtn.isVisible()) {
-      await firstTemplateBtn.click();
-      await editor.waitForCanvasReady();
-    }
+    await editor.goto();
   });

   test('should match dashboard screenshot', async ({ page }) => {
