Extract duplicate capability checks in haptics utility

Extracted redundant `if ('vibrate' in navigator)` checks and inconsistent `try/catch` blocks into a single shared `vibrate` helper function. This simplifies the module, standardizes error handling, and improves SSR/test safety by explicitly checking for `navigator`. Also added a comprehensive test suite for `utils/haptics.ts`.
