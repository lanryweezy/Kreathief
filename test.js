const normalize = (s) =, '').trim(); const sub = 'OWN THE NEON STREETS'; const head = 'OWN THE NEON STREET'; const cleanSub2 = normalize(sub).replace(/[a-z0-9]/g, ''); const cleanHead2 = normalize(head).replace(/[a-z0-9]/g, ''); if (cleanSub2 === cleanHead2) { console.log('true - exact'); process.exit(0); } const subWords = normalize(sub).split(/\s+/).filter(w =; const headWords = new Set(normalize(head).split(/\s+/)); const overlap = subWords.filter(w =; console.log('overlap', overlap, 'total', subWords.length, 'ratio', overlap / subWords.length); console.log('result', overlap / subWords.length  
